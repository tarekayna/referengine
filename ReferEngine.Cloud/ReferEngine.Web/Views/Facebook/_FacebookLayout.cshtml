@{
    ViewProperties viewProperties = ((ViewProperties)ViewData["ViewProperties"]);
}

<!DOCTYPE html>
<html lang="en">
    @RenderSection("CustomHead", false)
    <head>
        <title>Refer Engine - Marketing for App Developers</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="">
        <meta name="author" content="Apexa Inc.">
        <link href='https://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
        
        @Html.Partial("_SetJavaScriptGlobalVariables")
        @Html.Partial("_FavIcon")
        @Styles.Render("~/bundles/css/bootstrap")
        @RenderSection("styles", required: false)
    </head>
    
    <body>
@*        <div class="navbar navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <div class="pull-right navbar-logo-ctnr">
                        @Html.Image(Server.MapPath("~/img/logo-fb-web-header.png"), "Refer Engine - Real Customers Only!")
                    </div>
                </div>
            </div>
        </div>*@

        @RenderBody()

        @Html.Partial("_MixPanel")
        @Scripts.Render("~/bundles/developerLayout")
        
        @if (viewProperties.UseJQueryUi)
        {
            @Scripts.Render("~/bundles/libs/jquery-ui")
        }
        @RenderSection("scripts", required: false)
        @StackExchange.Profiling.MiniProfiler.RenderIncludes()
        @Html.Partial("_GoogleAnalytics")
    </body>
</html>
